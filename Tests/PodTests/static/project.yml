name: StaticPod
options:
  bundleIdPrefix: com.mapbox.staticpod
targets:
  app:
    type: application
    platform: iOS
    deploymentTarget: "12.0"
    sources: [Sources]
    settings:
      base:
        SWIFT_OBJC_BRIDGING_HEADER: Sources/app-Bridging-Header.h
